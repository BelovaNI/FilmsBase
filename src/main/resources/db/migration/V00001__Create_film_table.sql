CREATE TABLE IF NOT EXISTS film (id BIGINT PRIMARY KEY AUTO_INCREMENT, filmId BIGINT NOT NULL, filmName VARCHAR(1500), year INT, rating DECIMAL(3,2), description VARCHAR(3000), genres JSON NOT NULL, viewed BOOLEAN);
