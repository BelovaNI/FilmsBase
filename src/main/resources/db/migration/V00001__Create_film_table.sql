CREATE TABLE IF NOT EXISTS film (id BIGINT generated always as identity primary key, filmId BIGINT NOT NULL, filmName VARCHAR(1500), year INT, rating DECIMAL(3,2), description VARCHAR(3000), genres jsonb, viewed BOOLEAN);
